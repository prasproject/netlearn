/// NetLearn Design System — Dimensions & Spacing
class AppDimensions {
  AppDimensions._();

  // ── Border Radius ──
  static const double radiusSmall = 8.0;
  static const double radiusMedium = 10.0;
  static const double radiusLarge = 12.0;
  static const double radiusXL = 16.0;
  static const double radiusPill = 99.0;
  static const double radiusPhone = 30.0;

  // ── Padding ──
  static const double paddingXS = 4.0;
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 12.0;
  static const double paddingLarge = 16.0;
  static const double paddingXL = 20.0;
  static const double paddingXXL = 24.0;
  static const double paddingHeader = 28.0;

  // ── Gaps ──
  static const double gapXS = 2.0;
  static const double gapSmall = 4.0;
  static const double gapMedium = 6.0;
  static const double gapLarge = 8.0;
  static const double gapXL = 10.0;
  static const double gapXXL = 12.0;

  // ── Icon Sizes ──
  static const double iconSmall = 12.0;
  static const double iconMedium = 18.0;
  static const double iconLarge = 20.0;
  static const double iconXL = 32.0;

  // ── Progress Bar ──
  static const double progressHeight = 4.0;
  static const double progressHeightLarge = 5.0;

  // ── Button Shadow ──
  static const double buttonShadowOffset = 4.0;
  static const double buttonShadowSmall = 3.0;

  // ── Card ──
  static const double cardElevation = 0.0;
  static const double cardBorderWidth = 1.5;

  // ── Badge ──
  static const double badgeSize = 40.0;
  static const double badgeSizeLarge = 64.0;

  // ── Avatar ──
  static const double avatarSize = 36.0;
  static const double avatarInnerSize = 28.0;

  // ── Bottom Nav ──
  static const double bottomNavHeight = 60.0;

  // ── Score Ring ──
  static const double scoreRingSize = 80.0;
  static const double scoreRingBorder = 5.0;

  // ── Mascot ──
  static const double mascotSize = 90.0;
  static const double mascotSizeSmall = 56.0;

  // ── Node (Simulation) ──
  static const double nodeIconSize = 36.0;
  static const double nodeIconRadius = 10.0;
  static const double packetSize = 10.0;
}
